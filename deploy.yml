client:
  type: gradio
  parameters:
    interface_args:
      title: clmbot
      description: A causal language model bot
    launch_args:
      inbrowser: True

model:
  path: "~/.clmbot/model/"

generation_args:
  min_length: 100
  max_length: 500
  top_k: 50
  top_p: 0.95

client:
  type: cli
  parameters: {}

tokenizer:
  type: gpt2
  parameters:
    pad_token: "50256"

model:
  path: ./results/model/

generation_args:
  do_sample: True
  min_length: 1024
  top_k: 15
  top_p: 0.95

client:
  type: cli

tokenizer:
  type: EleutherAI/gpt-neo-125M
  parameters:
    pad_token: <|endoftext|>

model:
  path: "~/.clmbot/model/"

generation_args:
  do_sample: True
  min_length: 512
  max_length: 1024
  top_k: 50
  top_p: 0.95
